/** @format */

import { Stack } from "expo-router";
import * as React from "react";

const Layout = () => {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
      }}
    ></Stack>
  );
};

export default Layout;
